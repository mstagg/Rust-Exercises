// any shared logic needed for integration tests can be configured like so...
// Rust will not automatically include test code that is configured in the old
// module format like this.
pub fn setup() {}
